hero_card = {
    "title": "CS API — Public Counter-Strike Pro Play Data [REST]",
    "description": """
    CS API provides free, public access to Counter-Strike Pro Play match data,
    teams and player information of the top 100 teams.
    No API key is required — simply call the endpoints to get JSON responses in seconds.
    """
}